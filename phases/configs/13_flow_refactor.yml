skill: flow-refactor
description: Refactor by endpoint flow — controller→service→repository with full chain context and behavioral repair loop
tool: flow
skill_name: java-flow-refactor
skill_section: LLM INSTRUCTIONS
review_criteria: |
  The diff must improve code quality (guard clauses, method extraction, controller lean, DIP)
  while preserving the HTTP contract of all affected endpoints.
  REJECT if: public method signatures changed, HTTP mappings modified (@GetMapping, path),
  new imports of classes not present in the project, different runtime behavior,
  or [SHARED] files had their signatures changed.
