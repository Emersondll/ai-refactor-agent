skill: clean-imports
description: Remove unused imports and order imports alphabetically
tool: openrewrite
artifact_coordinates: []
recipes:
  - org.openrewrite.java.RemoveUnusedImports
  - org.openrewrite.java.OrderImports
review_criteria: |
  The diff must ONLY remove unused import lines and reorder imports.
  REJECT if: any Java code line other than imports was changed,
  or any valid import was removed by mistake.
