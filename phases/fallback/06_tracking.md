# Phase 06 — Cross-File Reference Update

Update this file to reflect renames introduced in phase 04.

## What you MUST do

- Update every import that references a class renamed in a previous phase.
- Update every `@Autowired` field that references a renamed class.
- Update every method call that references a renamed method from another class.
- Update every `extends` or `implements` clause that references a renamed type.
- Update every constructor call that references a renamed class.
- If no rename from a previous phase affects this file, make no changes.
- Do NOT introduce new renames in this phase.